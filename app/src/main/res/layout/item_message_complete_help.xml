<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:paddingVertical="12dp"
        tools:background="@color/dore_white">

        <ImageView
            android:id="@+id/iv_message_complete_help"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_complete_message_check"
            app:layout_constraintEnd_toStartOf="@id/tv_message_complete_help"
            app:layout_constraintHorizontal_chainStyle="packed"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintHorizontal_bias="0.43"/>

        <TextView
            android:id="@+id/tv_message_complete_help"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:fontFamily="@font/spoqa_han_sans_neo_medium"
            android:text="@string/message_complete_help"
            android:textColor="@color/dore_text_black"
            android:textSize="16dp"
            app:layout_constraintBottom_toBottomOf="@id/iv_message_complete_help"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/iv_message_complete_help"
            app:layout_constraintTop_toTopOf="@id/iv_message_complete_help" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
